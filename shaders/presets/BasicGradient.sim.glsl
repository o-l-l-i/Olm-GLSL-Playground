// BasicGradient.sim.glsl
//
// Blank simulation shader - no simulation needed for this effect.

precision mediump float;
uniform vec2 iResolution;

void main() {
    gl_FragColor = vec4(0.0, 0.0, 0.0, 0.0);
}